reference/
__pycache__/
*.pyc
.scratch/
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache/
.obsidian/graph.json
.obsidian/hotkeys.json
# Go build output. `go build ./...` writes the command's binary into the module
# root, and two of them reached a commit before this line existed. The binary
# provenance gate that would have caught it is rebuild-plugboard task 3.14.
/sidecar/sidecar
/terminator/terminator
/conformance/conformance
conformance/dist/
# Agent handoff documents: working notes one session leaves for the next, written by
# the handoff skill. They stay in the working copy and never reach a commit.
/handoff/
