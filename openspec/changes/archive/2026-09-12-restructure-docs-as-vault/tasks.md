## 1. Vault substrate and the gate harness

- [x] 1.1 Declare the closed root set in one retrievable location, naming each root **governed** (the spine, the planning directories, the per-component directories) or **exempt** (with a recorded reason stating what owns its files instead), plus the entry-point set and the out-of-scope set every later gate keys on; add a gate that fails when a tracked markdown file falls under neither, that fails when a directory is omitted from a traversal without being declared exempt, and that reports the roots it covered and excluded on every run including a passing one; verify with four cases — a note at an undeclared path failing with that path and the declared set named, a tracked file under a declared exempt root being subject to no obligation of this change, an undeclared exclusion failing and naming the directory it requires be declared, and a passing run stating its own coverage — and assert the declared set accounts for every tracked markdown file in the repository exactly once, so that a scanner's configuration can never be what decides a gate's subject.
- [x] 1.1a Enumerate candidate files from the version-control index rather than from a filesystem walk, so that the gates' subject is the tracked set their rules are written about; verify an untracked markdown file under the spine is not gated, a tracked one outside every declared root is, and record whichever ignore-file or exclusion entry the agent scratch directory convention in `CLAUDE.md` requires so a prescribed working path cannot fail a gate about tracked files.
- [x] 1.2 Track the vault reader configuration that changes link resolution and note classification, and ignore per-person workspace state and caches; verify by opening the vault, moving a pane and confirming no tracked file changes, and by a check that fails when a workspace-state or cache path is added to version control.
- [x] 1.3 Create the violating-input harness — one directory holding a deliberately broken input per gate, each naming the gate it exercises and that gate's rule note — plus a meta-check that every declared gate has a violating input and actually fails on it; verify by adding a gate with no violating input and observing the meta-check fail, then by neutering one gate's logic and observing its violating input stop failing (the reference's singular-versus-plural `--warnings-as-errors` flag is the failure being prevented).
- [x] 1.4 Implement the link resolution gate over every tracked note, every component README and the contributor entry files, covering both missing files and missing heading anchors, reporting every unresolved link in one run; verify with a violating input carrying one link to a nonexistent file and one to a missing anchor, asserting both are named in a single run and that the report distinguishes the two causes.
- [x] 1.5 Implement the wikilink and transclusion prohibition gate; verify with a violating input containing one wiki-style link and one transclusion, asserting each failure names the note, the line and the equivalent relative-markdown form.
- [x] 1.6 Implement the frontmatter gate over the spine and the per-component roots only, the planning directories being declared out of its scope in task 1.1's set with their reason recorded — `type`, `status` and `authority` required, each from a closed enumeration held in one location that names the note class every value covers, every missing field named at once, a value outside its enumeration refused, a note class with no assigned value refused, and a field no gate and no index generator reads refused; verify with violating inputs for each of the six cases, asserting the two-missing-field case names both in one failure and that a planning artifact carrying no frontmatter does not fail this gate while still being covered by the link and reachability gates.
- [x] 1.7 Extend the frontmatter gate to refuse a spine note declaring normative authority; verify with a violating input in the spine declaring it.
- [x] 1.8 Implement the reachability gate — every note reachable by links from a declared entry point, reported as a link path — and make it detect both a newly added unlinked note and a note orphaned by a deletion; verify with two violating inputs covering both cases, and by asserting the passing report can state a path from an entry point to an arbitrary note.
- [x] 1.9 Implement the index generator and its drift gate — generated indexes tracked, byte-identical to a fresh regeneration, each declaring that it is generated and naming its tool, with a hand edit detected as drift; verify by changing a note without regenerating and observing failure, and by hand-editing a generated index and observing a drift failure that names the tool.
- [x] 1.10 Implement the duplication gate that fails when requirement text appears both in the spine and in a specification; verify with a violating input copying one requirement paragraph into a spine note, asserting the failure names both locations.
- [x] 1.11 Add one aggregating target that runs every gate in this change with a report-only mode, and record which gates are blocking at which point in this task list; verify the target exits non-zero when any gate fails and zero in report-only mode, and that its output enumerates every gate it ran, so the gate inventory is a build output rather than a prose count that can drift as it already has.
- [x] 1.12 Implement the out-of-scope assertion over task 1.1's declared set — every path in it byte-unchanged across this change — and wire it into the aggregating target so it runs with the other gates rather than as a closing review step; verify by modifying one byte of a path in the set and observing a failure that names the path and the change that declared it out of scope, and assert the set contains `rebuild-plugboard`'s sixteen specification files, which the coherence pass of the tool this change invokes seven times could otherwise revise unnoticed.

## 2. One decision register

- [x] 2.1 Enumerate every decision in `docs/05-decision-log.md` and classify each as recorded in `design.md` under a different identifier or recorded nowhere else, producing a table of both; verify the table accounts for all fifteen entries and that each "recorded elsewhere" row names the `design.md` identifier it maps to.
- [x] 2.2 Fold the seven decisions recorded only in `docs/05-decision-log.md` — rebuild-from-scratch, one-repository, contract-first-with-conformance, version-skew-as-governing-constraint, the positioning, WebTransport-deferred, observability-before-the-hot-path — into `rebuild-plugboard`'s `design.md` as D21 to D27 using `/opsx:update`; verify `openspec validate rebuild-plugboard --strict` passes and each new decision carries its rationale and its alternatives.
- [x] 2.3 Write the collision table note recording, for every identifier the superseded register used, what it denoted there and what it denotes now; verify a citation written against the old register resolves through the table for all fifteen identifiers.
- [x] 2.4 Retire D11 to D15 as permanently unassigned, recorded with the reason, and add a gate refusing assignment of a retired identifier; verify with a violating input assigning D13, asserting the failure names the identifier and its former meaning.
- [x] 2.5 Name the surviving register in task 1.1's retrievable location and add the single-register gate whose subject is *an identifier in the installation's unnamespaced `D<n>` sequence introduced anywhere other than the declared register* — not "a `D-` heading outside `design.md`", which would either fail this tree or license every future change to open its own `D1`–`D10`; verify with violating inputs for an unnamespaced identifier in a second permanent location and for one in a change's own design artifact, that a change's namespaced decisions pass and are not treated as a register, and that citing an identifier the register holds — the collision table in 2.3, the decision notes in 2.7, a task, a specification — passes.
- [x] 2.6a Assert this change's own decisions are namespaced: `restructure-docs-as-vault`'s ten decisions are `RDV1`–`RDV10`, having been `D1`–`D10` colliding one-for-one with `rebuild-plugboard`'s; verify task 2.5's gate passes over this change's own artifacts, that no artifact of this change cites an unnamespaced identifier it does not own, and that the namespacing rule is stated where a future change will read it before numbering its own.
- [x] 2.6 Add the enumeration check that a register cannot be removed while it records a decision recorded nowhere else, then delete `docs/05-decision-log.md`; verify the check fails against the pre-merge state naming all seven at-risk decisions, and passes after task 2.2.
- [x] 2.7 Write the decision index and one note per decision in force, each linking into its `design.md` entry rather than restating it, and each linking to the capabilities and rules it shapes; verify the index enumerates D1 to D10 and D16 to D27, names D11 to D15 as retired, and that no decision note restates its `design.md` text.
- [x] 2.8 Correct `tasks.md:3` in `rebuild-plugboard`, which cites "D2 and D4" for the single-repository directory shape and resolves coherently only by mixing registers, via `/opsx:update`; verify the rewritten task cites identifiers that all resolve in the surviving register and that the cited decisions actually require what the task does.

## 3. The structural move

- [x] 3.0 Fix the three classification enumerations before any note is written or moved, assigning a `type`, `status` and `authority` value to every note class this change creates — orientation essay, concept note, decision note, glossary, rule note, banned-pattern note, procedure, generated index, map-of-content, entry file — and apply the frontmatter to every file this section moves or creates as it is moved or created, not afterwards; verify no note class is left without a value, that the enumeration lives in the single location task 1.6's gate reads, that no spine note carries normative authority, and that the frontmatter gate passes over the whole spine at the end of this section. Without this task the gate built in 1.6 has nothing to pass over: eleven existing `docs/` files begin with an H1 and carry no frontmatter, and no other task assigns a value to any of them.
- [x] 3.1 Create the spine tree — `why/`, `how/`, `capabilities/`, `decisions/`, `code/`, `history/`, `method/` — with a generated index per directory; verify the generator produces every index and the drift gate passes.
- [x] 3.2 Write `start-here.md` carrying the ordered reading path that filename numbering currently encodes, with a "read this when" column, taking over `docs/README.md`'s two load-bearing pieces — the reading order and the five governing facts — and then retire `docs/README.md`, moving its remaining orientation content into `why/` and deleting it, since two indexes are the drift this change exists to remove; verify every path entry resolves, the reachability gate passes from it as an entry point, it names the five governing facts, that no tracked file still links to `docs/README.md`, and that each of its eleven relative links is accounted for by task 3.9's inventory.
- [x] 3.3 Split `docs/01-system.md` into `why/` — what-it-is, audiences, version-skew, scope-refusals — preserving every refusal with its reason and every citation; verify no sentence is lost by a word-count reconciliation against the original, and that the four notes are reachable and their links resolve.
- [x] 3.4 Split `docs/02-architecture.md` into `how/` — architecture, three-primitives, the-tunnel, topology — preserving the frame vocabulary and the ASCII diagrams verbatim; verify the diagrams are byte-identical to the originals and every standards citation survives.
- [x] 3.5 Move `docs/04-protocol-fidelity.md` into `how/protocol-fidelity.md` with its three tiers intact; verify the tier structure and every per-family note survive, and that links from `how/` notes to it resolve.
- [x] 3.6 Move `docs/03-reference-audit.md` into `history/reference-audit.md`; verify all blocking and structural defects survive with their citations and that the citation gate passes for it once task 4.3 lands.
- [x] 3.7 Move `docs/06-carry-forward.md` into `history/carry-forward.md` as one note, adding a stable anchor per item and an anchor index; verify every item has a unique anchor, the anchor count matches the item count, and a link to each anchor resolves.
- [x] 3.8 Split `docs/07-methodology.md` by enumerating **every one of its thirteen top-level sections by name** and naming exactly one destination for each — the finding-to-constraint table, pull requests and the PR template, "what breaks if this is wrong", wire-contract impact, tests, the review checklist, code review, AI-generated PR review, testing, changelog and versioning, documentation, the AI harness, supply chain, and contributor experience — with the code-facing and test-facing parts going to `code/` as rule notes per task 6.12 and the rest to `method/` as documentation-rules, harness and supply-chain; verify by a section-by-section reconciliation asserting each named section appears in exactly one destination and none is dropped, that the duplication gate passes, and that every item of the review checklist and every change-description question lands in exactly one location. A three-destination split verified only on the checklist cannot detect the loss of a section, and the four test-shape prohibitions are the sections most likely to be lost.
- [x] 3.9 Repoint all **thirty-nine** references across the **ten** pre-existing files that name a numbered essay, in two classes with two detectors: the fifteen `docs/NN-*.md` occurrences in `CLAUDE.md`, `docs/07-methodology.md`, `rebuild-plugboard/design.md` and `rebuild-plugboard/tasks.md`, which sit in backticked prose no link gate can see and are caught only by a path search; and the twenty-four bare relative links of the form `](02-architecture.md)` inside `docs/` itself — eleven in `docs/README.md`, the rest across `docs/01`, `docs/02`, `docs/04`, `docs/05`, `docs/06` and `docs/07` — which are real markdown links inside the link gate's scope. Use `/opsx:update` for the two planning artifacts, and reject any spec revision its coherence pass proposes, recording it as follow-up work. Verify the link gate reports zero unresolved links, that a search for both the prefixed and the relative form finds no remaining path, and that the count repointed equals the inventory so a class of reference cannot be missed the way the relative form was.
- [x] 3.10 Delete `docs/agents/`, having first moved its glossary role to task 4.1's glossary; verify no tracked file links to the deleted paths, the link gate passes, and no remaining note mandates a decision system competing with the surviving register.
- [x] 3.11 Flip the link, wikilink and frontmatter gates from report-only to blocking — the frontmatter gate only because task 3.0 applied the values it checks — and hold the reachability gate in report-only until task 5.6, because the sixteen specifications acquire their first inbound link from the concept notes in section 5 and a reachability gate made blocking here would fail on all sixteen and put every task in section 4 on a red tree; verify the aggregating target exits zero on the current tree, non-zero for each violating input, and that its output names the count of files each newly blocking gate passed over, so a gate that passed by covering nothing is distinguishable from one that passed by being satisfied.

## 4. The notes that do not exist today

- [x] 4.1 Write the glossary defining every domain term the specifications use — mount point, tenant, exchange, projection, affinity, principal, credential, claim, terminator, frame, stream, capability, and the instance/installation/node triple the decisions fix; verify every term the sixteen specifications use in a defining sense appears, checked by a term-extraction pass whose output is reconciled against the glossary, and that a term added to a specification and absent from the glossary fails.
- [x] 4.2 Write `history/obtaining-the-reference.md` stating how to obtain the prior-art repositories and pinning the exact revisions the citations were taken against; verify by following the instructions from a clean environment and confirming a sample of ten of the 139 citations resolves to the cited lines at the pinned revisions.
- [x] 4.3 Implement the citation gate — every behavioural claim carries a location in a tracked file, a named test, or a citation into an artifact whose obtaining instructions and pinned revision exist — and the obtainability gate that refuses a citation into an artifact with no such note; verify with violating inputs for an uncited claim and for a citation into an unobtainable artifact, asserting the second names the artifact.
- [x] 4.4 Write `why/alternatives.md` positioning against ngrok, cloudflared, frp, Tailscale Funnel, and a dynamic-configuration reverse proxy, stating for each what it does that this does not and what this does that it does not; verify each claim about another product carries a citation to that product's own documentation, and that cloudflared's appearance here is distinguished from its appearance as runtime evidence in the decisions.
- [x] 4.5 Write `how/threat-model.md` consolidating the adversaries, assets and controls currently scattered across the specifications — request smuggling, SSRF, cross-tenant cookie scoping, certificate issuance escalation, cross-tenant writes, open relay, compression amplification, credential custody; verify each entry links to the specification owning its control and that a control with no owning specification is named as a gap rather than omitted.
- [x] 4.6 Write `how/failure-taxonomy.md` enumerating the failure classes a client, a tenant and an operator can observe — the six timers, refusals, resets, and the error space — each linking to its owning specification; verify the six timer classes and every enumerated error condition in the specifications appear, keyed so that adding one to a specification fails until the taxonomy names it.
- [x] 4.7 Write `how/operator-view.md` covering what an operator installs, what they configure, what they watch, and what a stuck tunnel looks like from their side; verify every diagnostic and signal it names exists in a specification requirement, and that anything not yet built is marked planned per task 7.3.
- [x] 4.8 Write `how/tenant-view.md` covering what a tenant installs, configures and verifies, and what they see when a capability their backend needs is refused; verify every configuration value it names exists in the sidecar's configuration schema requirements and that the refusal behaviour it describes links to the negotiation requirement rather than restating it.
- [x] 4.9 Write `why/success-criteria.md` stating the bar for each of the three named audiences, including the portfolio audience the system document names without a criterion; verify each criterion is observable and names how it would be judged met.
- [x] 4.10 Write `history/research-provenance.md` recording what was investigated, by how many passes, which conclusions were upheld and which overturned, and the fabricated citations the adversarial pass caught; verify each overturned conclusion links to the decision that superseded it and that the fabrication findings carry their verification method.

## 5. The capability layer

- [x] 5.1 Define the concept-note shape and declare its size ceiling, writing the first four notes to set the value empirically; verify the four are under the declared ceiling and each links to its specification, its dependencies and the decisions that shaped it.
- [x] 5.2 Write the remaining twelve concept notes, one per capability, restating no requirement; verify all sixteen are under the ceiling, the duplication gate passes, and each names what the capability owns and what it explicitly does not.
- [x] 5.3 Add the dependency correspondence gate — a capability named as a dependency inside a specification with no matching link in that capability's concept note fails; verify against the eighty existing cross-capability references, asserting the gate passes only once every one is linked, and with a violating input adding an unlinked dependency reference.
- [x] 5.4 Add the gate refusing a specification whose capability has no concept note, and a concept note for a capability with no specification; verify with a violating input in each direction.
- [x] 5.5 Verify the navigational payoff directly: assert that the set of capabilities depending on a given capability is obtainable from that concept note's inbound links without opening any specification, checked for the three most-referenced capabilities.
- [x] 5.6 Flip the reachability gate from report-only to blocking, now that the concept notes give every specification an inbound link from a note and the planning artifacts are reachable through them; verify every note and every planning artifact is reachable from a declared entry point with the report stating a link path for each, that a planning artifact's inbound link comes from a note rather than from another planning artifact, and that the aggregating target exits zero on the whole tree with reachability blocking.

## 6. The code guide

- [x] 6.1 Write one boundary note per component stating what it owns and what it must not own, keyed on the set of components that exist; verify a gate fails when a component directory has no boundary note, when a boundary is restated rather than linked, and when a boundary note survives its component's removal, each demonstrated against a violating input.
- [x] 6.2 Define the rule-note shape and generate the rule index carrying each rule's enforcement status; verify the index lists every rule note, states each one's gate or no-gate marker, and that the proportion carrying no gate is retrievable from it.
- [x] 6.3 Implement the bidirectional rule-to-gate correspondence check — a gate naming no rule note fails, a gate naming a nonexistent rule note fails, a rule note claiming a gate that is not run fails, and a rule note with neither a gate nor the marker fails; verify with a violating input for each of the four cases.
- [x] 6.4 Establish the ungated-rule quarantine, marked as preference and barred from being cited as a blocking objection; verify a gate fails when an ungated rule is placed among the gated ones, and that enforcement status is readable from the index without opening a note.
- [x] 6.5 Split the banned patterns into one note each with stable anchors, each naming the defect it prevents with a citation into a tracked file, a named test, or the pinned prior art; verify every pattern has a unique anchor, every one carries a demonstrated defect, and violating inputs cover a pattern stated without an anchor and one with an uncited defect.
- [x] 6.6 Make every gate's failure output name the rule note it enforces; verify by running each gate against its violating input and asserting the rule note path appears in the output, and that moving a rule note without updating its citing gate fails the correspondence check.
- [x] 6.7 Write the adding-a-feature procedure naming the ordered artifacts to touch, where the change is specified, where it is proven failing first, where the code goes, what is updated alongside, and its own verification; verify a gate fails when the procedure names a command, target or directory the repository does not provide.
- [x] 6.8 Write the fixing-a-bug procedure requiring the failing case be demonstrated before the fix, and prohibiting a test that asserts defective behaviour and a helper that performs the transition it waits for; verify the procedure's own verification is stated, and that review refusals for the three prohibited shapes each cite it.
- [x] 6.9 Write per-language convention notes for every language holding tracked source — formatter, linters, size ceilings, banned constructs — keyed on the languages present; verify a gate fails when tracked source appears in an uncovered language, when a covered language has no source, and when a note restates a convention the formatter already enforces.
- [x] 6.10 Single-source the review checklist, the change-description questions and the set of blocking objections, keyed on their enumerations; verify adding one checklist item fails until every artifact required to enumerate it names the item, with the failure listing each artifact still missing it, and that a restated checklist fails.
- [x] 6.11 Set the module and function size ceilings and the duplication threshold to values the pinned prior art demonstrably violates, citing the violating locations; verify each threshold is stated with its citation and that the prior art's cited cases would fail it.
- [x] 6.12 Write the testing obligations as rule notes in the same shape as every other rule — the test tiers and what each is for, the fast-tier latency budget as a correctness control rather than a convenience, the conformance suite's authority over any implementation's own tests, and one note each with its own stable anchor and cited defect for the four prohibitions on test shape: a helper that performs the state transition it is written to wait for, a test asserting defective behaviour rather than intended behaviour, a security or correctness property weakened or disabled in test configuration, and an uncertainty marker or unconditional skip standing in for a failing assertion; verify each appears in task 6.2's rule index with its enforcement status, that the correspondence check in 6.3 covers all four, that a violating input holding one of them as prose in a methodology note fails, and that each is citable as a blocking objection under 6.4's quarantine rule — which, left as prose, would have retired four existing review blockers by relocating them.

## 7. Currency and precedence

- [x] 7.1 State the five-layer authority precedence in exactly one location and add the gate that fails when it appears elsewhere with differing content; verify with a violating input restating it, and that every note describing behaviour declares at its head that the owning artifact wins.
- [x] 7.2 Add the gate refusing a spine note that states behaviour a specification owns without marking the statement non-normative and linking to that specification; verify with a violating input, asserting the failure names both the note and the owning specification.
- [x] 7.3 Add the currency gates in **both** directions — a note whose subject was removed fails, a superseded note must name its replacement, a note describing an unbuilt artifact must be marked planned and link to the work that builds it, and **a note still marked planned whose named subject now exists in the tree, or whose linked task is complete, fails until the marker is retired**; verify with a violating input for each of the four, plus one for a planned marker naming no path the gate can test. The fourth is the direction that will actually fire: this change lands before the task that creates `contract/`, `proxy/`, `sidecar/`, `terminator/` and `conformance/`, so without it nearly the whole spine would stay stamped planned against directories that appear immediately afterwards, with the gate green and `status: planned` the default value an agent reads to decide whether a note can be trusted.
- [x] 7.4 Apply the planned marker across every note describing something the repository does not yet contain, which is most of the spine today, each marker naming its subject as a path the gate can test for existence and linking to the task or change that builds it; verify the currency gates pass over the whole tree in both directions, that every marker's subject path is testable, and that planting a subject directory for one marked note makes that note fail until its marker is retired.

## 8. Entry points and the authoring hook

- [x] 8.1 Rewrite `CLAUDE.md` as a router — links into the vault, at least one invariant, at least one banned pattern, no framework conventions — under the declared ceiling it currently exceeds at 4,865 bytes; verify the size gate passes, every link resolves, and a check fails when a framework-conventions section is added.
- [x] 8.2 Write `AGENTS.md` as the counterpart entry file under the same obligations; verify both entry files pass the size, link and framework-conventions gates, and that neither restates the other.
- [x] 8.3 Add the absent-artifact gate for entry files, refusing an instruction to use a generated index, tool or directory the repository does not contain; verify against the current `CLAUDE.md`, which instructs an agent to query a knowledge graph that has never been generated, asserting the gate fails before the fix and passes after, and that the tool hook firing on that absent graph is removed or gated on its existence.
- [x] 8.4 Populate all three of `openspec/config.yaml`'s blocks, entirely commented out today — `context:` and `rules:` with the conventions that apply to authoring a planning artifact, and `operations:` with the guidance that governs the apply and archive loops, which are what will touch this vault across 709 tasks; verify by generating a planning artifact and confirming the conventions are present in the context the workflow reads, and by confirming the apply and archive guidance is present in the operations context.
- [x] 8.4a Add the second authoring channel, because `config.yaml` cannot carry a per-note obligation — its `rules:` block is keyed per openspec artifact — so every per-note convention would otherwise remain a review-time hope: a location a note author reads before writing one, either a hook or a conventions note that both entry files link as their first hop, generated from the same single convention source as the gates; verify that authoring a note surfaces the conventions before the first write, that the location is not `config.yaml`, and that the two channels cannot state differing conventions.
- [x] 8.5 Key both authoring channels and the gates on one enumeration derived from the single convention source, so a convention added to the gates without reaching both channels fails; verify with a violating input adding a gate-side convention only, asserting the failure names the convention and the channel missing it, and with one making the two channels disagree, asserting the failure names both.

## 9. Reconciling the change already in flight

- [x] 9.0 State the discipline every `/opsx:update` invocation in this change observes, once, and key the seven invocations on it — tasks 2.2, 2.8, 3.9 and 9.1 to 9.4: the tool applies the requested edit and then reconciles neighbouring artifacts *in any direction* over glob-expanded specification paths, forbidding only their creation and not their revision, so any spec revision its coherence pass proposes is refused and recorded as follow-up work naming the artifact and the proposed revision; verify each of the seven invocations cites this discipline rather than restating it, and that task 1.12's out-of-scope assertion runs after each one.
- [x] 9.1 Rewrite `rebuild-plugboard` task 1.1 via `/opsx:update` so the directory shape cites the surviving register and the per-directory READMEs point at task 6.1's boundary notes rather than restating boundaries; verify `openspec validate rebuild-plugboard --strict` passes and the duplication gate would fail a README that restates a boundary.
- [x] 9.2 Rewrite task 1.3 via `/opsx:update` against the entry files, ceilings and gates this change delivers, removing the obligations it now duplicates; verify the rewritten task names no gate this change does not provide and that its size and content checks are the ones actually implemented.
- [x] 9.3 Rewrite task 1.4 via `/opsx:update` to adopt the link gate this change delivers rather than specifying a second one; verify only one link gate exists in the repository and the aggregating target runs it.
- [x] 9.4 Rewrite task 1.10 via `/opsx:update` so the docs gate watches the new spine paths and the marker convention it requires matches the one in force; verify a planning-artifact-only change and a contract-touching change each get the expected outcome from the rewritten gate.
- [x] 9.5 Verify both changes together: `openspec validate rebuild-plugboard --strict` and `openspec validate restructure-docs-as-vault --strict` both pass, no task in either names a path, gate or decision identifier that does not resolve, and **`rebuild-plugboard`'s sixteen specification files are byte-unchanged across the whole of this change** — asserted by task 1.12's gate over the declared out-of-scope set, not by inspection, since seven `/opsx:update` invocations each had the opportunity to revise them and only their creation was ever forbidden.

## 10. Closure

- [x] 10.1 Run every gate in blocking mode over the whole tree and every violating input through the meta-check; verify the tree passes, each violating input fails its own gate **by its own logic** (proven by neutering the violation-recording path and requiring the gate to then exit zero), that no violating input fails another **per-file** gate, that the run's output enumerates every gate and the roots each covered and excluded, and that the out-of-scope assertion is among the gates reported and passing. **Narrowed during application, with the reason recorded:** the original clause asked that no violating input fail *any* other gate, which is unsatisfiable — a minimal fixture necessarily fails every whole-tree gate because it has no entry point, no generated index and no reader configuration, and the unscoped check produced 165 failures, none of them a defect. Satisfying it literally would mean building one complete valid vault per fixture. Whole-tree gates are therefore declared in `ci/vault.json` and excluded from the isolation check; the property that makes a demonstration unambiguous — a links fixture must not trip the wikilink gate — is kept.
- [x] 10.2 Verify the vault opens in the reader with backlinks and graph populated from relative markdown links alone, with no wikilink present in any tracked note and no gate requiring the reader to run.
- [x] 10.3 Verify the reachability, currency and citation properties from a clean clone: every note reachable from an entry point, no note describing a removed subject, and every behavioural claim carrying a citation whose artifact is obtainable by the instructions in the tree.
- [x] 10.4 Verify the two navigational claims the change is for: a reader arriving with no context reaches the decision behind any capability in three links or fewer from `start-here.md`, and the set of dependents of any capability is obtainable without opening a specification.
