## 0. Standing discipline

- [x] 0.1 State once, and key every later task on it: this change touches no `rebuild-plugboard` specification. `openspec/changes/rebuild-plugboard/specs` stays in `ci/vault.json`'s declared out-of-scope set, and `ci/gates/out_of_scope.py` runs after every task below. Any `/opsx:update` coherence pass that proposes a spec revision is refused and recorded as follow-up work naming the artifact and the proposed revision. Verify by running `python3 ci/gates/out_of_scope.py` after each task and confirming it reports 16 of 16 specs byte-unchanged against the recorded baseline. Note the limit this discipline carries until task 2.5 closes it: the declared baseline is the moving ref `HEAD`, so the assertion catches an uncommitted edit and not a committed one.
- [x] 0.2 Create the follow-up record this change and its predecessor both promise but neither provides: a tracked note holding every refused spec revision and every deferred finding, linked from the vault so `ci/gates/reachability.py` reaches it. Seed it with the three findings already recorded as unfixable (the `auth/sidecar-credentials` isolation gap, the absent CORS owner, the 139 uncheckable prior-art citations) and with `restructure-docs-as-vault` task 9.0's obligation, which has had no destination since it was written. Verify `git ls-files | grep -ci follow` returns non-zero, that `python3 ci/gates/reachability.py` exits zero with the note tracked, and that the note names `restructure-docs-as-vault` task 9.0's obligation as one of the obligations it holds — the destination cites the origin, since the origin is a completed change's artifact and is not edited from here.
- [x] 0.3 Confirm all four of this change's planning artifacts stay reachable as they change. Each is linked from a note outside the planning directories — `proposal.md` and the code-standards delta from `docs/capabilities/docs-code-standards.md` and `docs/method/documentation-rules.md`, `design.md` and `tasks.md` from `docs/method/harness.md` — because `ci/gates/reachability.py` gives a change's own artifacts no grace period and caught this omission once already. Verify `python3 ci/gates/reachability.py` exits zero with all four of this change's artifacts tracked — the check that caught this exact omission once already, per `HVH1`'s risk entry.

## 1. Declarations, before any gate that reads them

Ordered first for the reason `ci/gates/_common.py` states in its own docstring: a gate never decides its own scope. A gate written before its declaration hardcodes a guess.

- [x] 1.1 Declare the runner path in `ci/vault.json` as `.github/workflows/check.yml`, with the required triggers (`push`, `pull_request`) and the aggregating target it must invoke, per `HVH2`. Verify the key is present, that `python3 -c "import json; json.load(open('ci/vault.json'))"` parses, and that no gate file contains that path as a literal — the declaration is the only place it appears.
- [x] 1.2 *(declaration written; closes at task 4.3, which builds the completeness check its verification names)* Declare the generator map in `ci/vault.json`, keyed on the tool rather than on the artifact, naming all three generators under `ci/gen/` and, for each, its violating-input tree and its pinned expectation file. Verify the declaration names exactly the set `ls ci/gen/*.py` returns (three today, and `ci/gen/indexes.py` — which produces 13 of the 15 tracked generated artifacts and is currently declared nowhere) and that the completeness check fails when a name is removed.
- [x] 1.3 *(declaration written; closes at task 4.5, which builds the resolver check its verification names)* Declare the named correspondences in `ci/vault.json`, per `HVH3`: the rule set (the notes `ci/gates/rule_gate_correspondence.py` holds against the rows `docs/rule-index.md` publishes) and the blocking classification against the gate modules the repository runs. Each side names its own resolver and how its members are resolved, not merely a count. Verify each declared resolver can be invoked and returns a member list, and that a correspondence whose resolver cannot run is reported as a failure rather than a crash.
- [x] 1.4 *(entries written, and the count is three not two — `runner`, `correspondences`, `generators`; closes at task 4 as its own verification states)* Declare the new gates in `gate_policy.whole_tree_gates` with the reason recorded there, per the risk entry in `design.md`: no fixture tree contains a runner configuration or the new declarations, so without this both new gates fail on all 21 trees through `ci/gates/meta.py`'s isolation check. Verify by running `python3 ci/gates/meta.py` after task 4 and confirming `per-file cross-gate failures: 0`.
- [x] 1.5 Move `ci/gates/component_boundaries.py`'s vacuity reason out of its docstring and into `ci/vault.json`, naming the reason and `rebuild-plugboard` task 1.1 as the work that ends it, per `HVH7`. Verify the gate's coverage line prints both from the declaration, and that deleting the declaration makes the gate fail rather than pass silently.

## 2. The two live defects, which need no new gate

- [x] 2.1 Widen `ci/gen/rule_index.py` to hold any note a gate's `RULE_NOTE` names, not only notes under `code_standards.rule_dirs`, per `HVH9`. Regenerate. Verify `sed -n 17p docs/rule-index.md` and `python3 ci/gates/rule_gate_correspondence.py | grep 'rule notes:'` now report the same three figures, that `grep -c authority-precedence docs/rule-index.md` returns non-zero, and that `docs/method/conventions.md`'s claim that all 15 conventions appear in the rule index is true for `precedence-once` — the knock-on that is false today.
- [x] 2.2 Correct the four `GATE.md` files whose stated violation count disagrees with what their gate emits: `citations` states three and emits four, `currency` states four and emits three, `index-drift` states four and emits six, `rule-gate-correspondence` states seven and emits eight. Verify each stated count equals the emitted count by running each gate against its own tree and comparing, and record the reason each drifted so the same drift is recognisable next time. Two of the four were not wrong numbers but defects the numbers hid, and both are prerequisites for task 4.4 rather than optional extras: `currency`'s fourth case was silent because the task id sat in prose rather than in the link label the gate reads, so the reverse-direction property this change is sold on was undemonstrated; and `index-drift` checked a declared generator's existence against fixture trees that carry no `ci/` at all, the same scope defect fixed in `component_boundaries` at task 1.5. A fifth file, `component-boundaries`, was added to the set by task 1.5 and corrected here.
- [x] 2.3 Give `ci/gates/index_drift.py` a coverage line naming all fifteen subjects it checks rather than the thirteen it names today — the two declared `generated_files` are checked and unreported. Verify the reported count equals the number of artifacts the run actually compared, and that adding a generated file to the declaration changes the reported count.
- [x] 2.4 Correct the two stale prose counts in `ci/vault.json`: "the sixteen gate modules" (there are 21) and "139 citations point into `reference/`" (that figure describes the pre-restructure tree; the current tree resolves more). Verify each corrected number against the command that produces it, and state the command beside the number so the next reader can recheck rather than trust.
- [x] 2.5 Pin the out-of-scope baseline to a fixed commit and demonstrate it. `ci/vault.json`'s `out_of_scope.baseline_ref` is `HEAD`, which the gate resolves per run, so the byte-unchanged assertion that four tasks across two changes depend on catches an uncommitted edit and passes a committed one — proven by appending a sentence to a frozen spec in a copied tree, which fails before the commit and passes after it. Set `baseline_ref` to `fb24a9f`, the commit at which the freeze began; all 16 specs are byte-identical between it and `HEAD`, so no verdict changes today. Add a violating input in which a frozen spec is edited **and committed**, and verify the gate fails on it — the case that passes today. Verify also that the gate still fails on an uncommitted edit, on a deletion, and on an untracked addition under a declared prefix, so the new baseline widens the assertion without dropping what it already caught.

## 3. The reporting surface

- [x] 3.1 Change `_common.Report` so the subject count is accumulated from subjects a gate registers as it examines them, rather than passed to `coverage()` as a literal. Verify every gate still reports the same count it reports today, and that a gate examining a subject without registering it is detectable — the twelve gates that hardcode a `from_index` value are the same population that hands over a literal count.
- [x] 3.2 Add the item kind to the coverage line, per `HVH6`. `_common.py` hardcodes the noun `file(s)`, so `ci/gates/precedence.py` reports "5 file(s)" for five precedence layers and `ci/gates/component_boundaries.py` "0 file(s)" for components. Verify each of the 21 gates names a kind that matches what it counted, by reading all 21 coverage lines against each gate's subject.
- [x] 3.3 Make the subject source three-valued — version control, a directory scan, or a named key in `ci/vault.json` — and correct the twelve gates that hardcode a value. Verify that no gate claims version control on a run whose root is not a work tree — the reporter now refuses that claim itself — and that the count of gates carrying a hardcoded literal, `grep -lE 'from_index=(True|False)' ci/gates/*.py | grep -vc _common`, falls to zero from twelve. The target of "zero gates report a filesystem scan" that this task first stated was wrong: four of the six genuinely scan (`capability-notes` globs specification files, `language-coverage` globs `ci/**/*.py`, `meta` walks fixture trees, `rule-gate-correspondence` globs the rule directories), and the defect was the LABEL — `scan` rendered as "filesystem (fixture tree)", a parenthetical written when scanning only ever happened on a fixture. It now reads "a directory scan". Verify the three source labels each describe what the run did.
- [x] 3.4 Add the coverage gate enforcing the requirement, with its violating input. Verify it fails on a gate claiming version control outside a work tree, on a gate whose failure messages name more distinct subjects than its reported count, on an undeclared empty subject set, and on a declaration whose named work is complete while the set is still empty — four cases, four failures, each demonstrated.

## 4. The new gates and their inputs

- [x] 4.1 Build the three generator violating inputs, each a small tree plus the exact output its generator is expected to produce on it, held **outside** `ci/broken-inputs/` per `HVH5`. Verify each generator run against its input matches its pinned file byte for byte, that altering one expression in a generator makes the comparison fail and name the first differing line, and that `ci/gates/meta.py` still reports its gate-to-fixture bijection intact — the reason these live outside that directory is that a fixture with no gate has no place in it.
- [x] 4.2 Have each generator state, in its own module, the class of wrongness its violating input does not reach, per `HVH4`. Verify each of the three carries such a statement and that a generator added without one fails — a partial oracle that does not say so is the defect this change exists to close, in a new costume.
- [x] 4.3 Build the generator gate, with its own violating input. Verify it fails on a generator whose output drifts from its pinned expectation, on a tool under `ci/gen/` the declaration does not name (true of `ci/gen/indexes.py` today), and on a tracked artifact declaring itself generated that no declaration accounts for.
- [x] 4.4 Build the fixture-declaration gate, with its own violating input. Verify it fails on a declared count that disagrees with the emitted count, on a declared case matching no emitted failure, and on a flag the accompanying note demonstrates that the declaration never exercises — `--self-test` is exactly that case: `ci/broken-inputs/out-of-scope/GATE.md` instructs that `meta` treat it as a second violating input, and `grep -rn -- '--self-test' Makefile ci/run-gates.py ci/gates/meta.py` returns zero. Verify the unit is the tree, not the directory: 21 trees live under 20 directories.
- [x] 4.5 Build the reconciliation gate, with its own violating input. Verify it fails naming the divergent **members** and not only the counts, in both directions; that it fails on a blocking-classification entry corresponding to no gate module; that a resolver which raises is reported as a failure rather than crashing the run; and that it would have failed on the 42-versus-43 divergence before task 2.1 closed it — run it against a tree with 2.1 reverted and confirm.
- [x] 4.6 Write one rule note per new gate, each naming its gate, and regenerate `docs/rule-index.md`. Verify `python3 ci/gates/rule_gate_correspondence.py` holds the correspondence in both directions with no gate naming an absent note and no note claiming an absent gate, and that the unenforced proportion the index publishes is the one the gate computes.

## 5. Narrow changes carrying no new requirement

- [x] 5.1 Have `ci/gates/decision_register.py` read its identifier prefix from `ci/vault.json` instead of hardcoding a literal `D` followed by digits. This carries no new requirement — the in-force "There is exactly one decision register" already states the rule. Verify the gate still catches everything it catches today: an unnamespaced assignment outside the register, a retired identifier revived (`D13`), a superseded register removed while holding a unique decision, and the collision table's completeness. Verify `RDV` and `HVH` still pass as namespaced, and that changing the declared prefix changes what the gate treats as the installation's sequence.
- [x] 5.2 Add `ci/gates/_common.py` to the enumerated review obligations `docs/code/reviewing.md` single-sources, per the risk entry that calls this the weakest mitigation in the change. State what a reviewer checks: that a change to `tracked_markdown` or `candidates` is verified against every gate's subject count, because narrowing one predicate collapses twelve gates' sets by roughly 80% while they all report `[ok]`, and fixture trees take the other branch so `ci/gates/meta.py` cannot see it. Verify `ci/gates/rule_gate_correspondence.py` and the review single-sourcing gate both accept the new obligation, and that it appears in `docs/rule-index.md` with its enforcement status stated honestly as a review obligation rather than a gate.

## 6. The runner, last

Ordered last for the reason `HVH1`'s migration plan gives: a runner introduced against a red tree teaches everyone to ignore it.

- [x] 6.1 Confirm `make check` is green on the tracked tree before writing any runner configuration. Verify from a clean materialisation of the index — `git checkout-index` into an empty directory, initialise a work tree there, and run the suite — rather than from the working tree. This is not ceremony: a half-staged rename in this repository's own history passed a working-tree check, failed the index, and went unnoticed through a completion report.
- [x] 6.2 Write `.github/workflows/check.yml` at the path task 1.1 declared, invoking the aggregating target on `push` and `pull_request`, with no path filter, no branch filter, and no clause allowing a non-zero exit to pass. Verify the file parses as YAML, that the triggers and the invoked target match the declaration exactly, and that the declaration — not the gate — is where the path comes from. The checkout must be unshallow: task 2.5 pinned the out-of-scope baseline to `fb24a9f`, and a shallow clone cannot resolve it, so the byte-unchanged assertion would fail on a missing ref rather than on a modified spec. Verify by resolving the pinned ref in the runner's own checkout.
- [x] 6.3 Assert the aggregating target's exit contract, which nothing guards today: `Makefile`'s `check` target is one unprefixed command, so a `-` prefix or a `|| true` added later would silence every gate. Verify by making one blocking gate exit non-zero in a copied tree and confirming `make check` exits non-zero and names that gate.
- [x] 6.4 Build the runner gate, with its violating input. Verify it fails when no configuration exists at the declared path, when the configuration carries a path filter, a branch filter, or a clause letting a non-zero exit pass, and that it does **not** claim anything about whether the hosting service refuses a merge — that is a server-side setting no tracked artifact states, declared out of scope in the requirement itself.

## 7. Closure

- [x] 7.1 Run every gate from a clean materialisation of the index and record the inventory: gates run, blocking, report-only, and each gate's subject count and item kind. Verify all blocking gates pass, that no gate reports zero subjects without a declaration naming the work that ends it, and that the count of gates hardcoding a subject source is zero.
- [x] 7.2 Verify the five requirements are each demonstrated by at least one violating input that fails for the stated reason, and that `python3 ci/gates/meta.py` reports the gate-to-fixture correspondence intact with zero per-file cross-gate failures. State the number of gates and the number of trees, and confirm the two disagree only by the gates carrying more than one scenario.
- [x] 7.3 Confirm `openspec validate harden-vault-harness --strict` reports valid and `openspec validate rebuild-plugboard --strict` still does, that `openspec list` still reports `rebuild-plugboard` at 0 of 709, and that `ci/gates/out_of_scope.py` reports all 16 specs byte-unchanged against the baseline recorded in `ci/vault.json`.
- [x] 7.4 Record in the follow-up note from task 0.2 anything this change deliberately left: the deferred conforming-input fixture set with its trigger (`rebuild-plugboard` task 1.1, per `HVH8`), and any refused spec revision. Verify the note names each item, its reason, and the work or event that ends it, so that a deferral is a scheduled obligation rather than a sentence in an archived proposal.
