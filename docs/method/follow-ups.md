---
type: moc
status: current
authority: none
---

# Follow-ups

The destination for work this repository has deliberately deferred. Two changes have required a
record like this and neither provided one: `restructure-docs-as-vault` task 9.0 obliges every refused
specification revision to be "recorded as follow-up work naming the artifact and the proposed
revision", and `harden-vault-harness` defers a fixture set on a named trigger
([its design](../../openspec/changes/archive/2026-09-12-harden-vault-harness/design.md), `HVH8`). Before this note, both
obligations pointed nowhere — `git ls-files` matched no file for either.

An entry names four things: what was deferred, where the obligation came from, why it was not done,
and the work or event that ends it. An entry with no ending condition is a wish, and this page is not
for wishes.

## Open

| deferred | origin | why not now | what ends it |
|---|---|---|---|
| Three shapes of one defect landed in one day, and none was declared to the gate that exists for it: the pull-request template against the note stating it, "which modules are not gates" in six hardcoded copies, and the test tiers' invocations in both YAML and make | Found while fixing each separately, 2026-09-12 to 2026-09-15 | Each was fixed where it was found. What was not done is the general question: `ci/gates/correspondences.py` reconciles a published enumeration against a computed one, and its `declared` set holds one pair. Three more existed in the tree and nobody had declared them, so the gate was green over a class it was built for | A pass over the tree asking what else is stated twice — and a decision about whether a second encoding should be declarable at all, or whether the answer is always to delete one of the two |
| Task **1.9** cannot be verified as its text describes. It asks for a bot-authored pull request touching `contract/` to be blocked from merge | `rebuild-plugboard` task 1.9, against the branch protection configured at task 1.6 | Blocking on CODEOWNERS needs `require_code_owner_reviews`, which needs an approving review. The repository has one maintainer and nobody can approve their own pull request, so approvals are set to zero — deliberately, because requiring one would mean no change could ever merge. CODEOWNERS requests the review; nothing blocks on it | A second reviewing account, or an explicit decision that requesting the review is the obligation and blocking on it is not |
| Tasks **3.11**, **3.12** and **3.13** — the dual-invocation parity check, its recurring schedule, and build-provenance stamping — are written and reverted. The work is on the branch `claude/unblocked-tasks-wip` | Two rounds of agent implementation plus adversarial review, 2026-09-14 | Each still had its core property demonstrably unguarded after the second round. The parity gate's scheduled-command clause was satisfied by a workflow that only `echo`s the command names; the provenance gate passed every one of its fixtures even when patched back to the plain-substring matching the first reviewer had condemned, so nothing guards its headline fix. Three of five stamped binaries bound only the component name, so their tests passed over a blank stamp. A gate nobody can trust is worse than no gate, and landing one green is the single failure this repository exists to prevent | Picking the branch up and closing the recorded findings — they are specific, reproduced, and each names the command that demonstrates it. The stamping itself is sound work; it is the guards around it that are not |
| `make check` now exceeds **120 seconds** at 39 gates and had to be run in the background. It was seconds at 26 gates, 68s at 33, 86s at 38 | Measured across 2026-09-12 to 2026-09-14 while adding gates | The cost is quadratic and structural: `ci/gates/meta.py`'s isolation check runs every per-file gate against every violating tree, so each gate added multiplies by each fixture added. This has gone from a trend to something that interrupts work — and this repository's own argument is that slow feedback changes what gets written | A decision about what the one command is for. Either a budget on it, the same shape as the fast tier's, or the cross-product moved to the recurring schedule with a cheaper property kept on change. It is a decision, not a tuning exercise |
| The harness is not held to its own size ceiling. Four gate modules are over the 300 non-comment-line ceiling `ci/gates/size_ceilings.py` now enforces: `ci/gates/out_of_scope.py` at 442, `links.py` at 340, `wikilinks.py` at 305, `citations.py` at 301 | Measured while building that gate at `rebuild-plugboard` task 3.5 | The task asks for the ceilings in "both languages", meaning the two the components are written in, and splitting four gate modules is a different piece of work from building the check. Python is declared out of scope in `ci/vault.json` with the reason, and the count is printed on every run rather than left to be discovered | A change that splits the four, after which `.py` is removed from `code_standards.code_size.excluded_languages` and the gate's own exclusion check fails if the declaration outlives it |
| `make check` now takes **86 seconds** (38 gates, measured 2026-09-12; it was 68 seconds at 33 gates earlier the same day, which is the trend rather than the number), and the cost is quadratic: `ci/gates/meta.py`'s isolation check runs every per-file gate against every violating tree, so each gate added multiplies by each fixture added | Measured while adding the section 2 and 3 gates; it was a few seconds at 26 gates | Nothing is wrong yet, and the isolation property it buys is real — a fixture tripping a second gate makes that gate's demonstration ambiguous. But this repository's own argument is that slow feedback changes what gets written ([the fast-tier budget](../code/rules/fast-tier-latency-budget.md)), and `make check` is the command run on every commit | A budget on `make check` itself, the same shape as the fast tier's — or an isolation check that runs the cross product on a schedule and a cheaper property on change. Either is a decision about what the one command is for, not a tuning exercise |
| Four of section 1's tasks are written and unticked, because their stated verification is a forge behaviour rather than a tree property: **1.5** (a pull request whose body ticks no wire-contract-impact box is refused), **1.8** (a synthetic patch bump auto-merges and a minor bump does not), **1.9** (a bot-authored pull request touching `contract/` is blocked from merge), **1.10** (a pull request touching `contract/` with neither a spine edit nor a marker is failed) | `rebuild-plugboard` [section 1](../../openspec/changes/rebuild-plugboard/tasks.md) | Every artifact exists — the template, `dependabot.yml`, the auto-merge workflow, `CODEOWNERS` and `ci/pr/checks.py` with eight recorded cases its `--self-test` runs. What cannot be done from a working copy is observing the forge act on them, and `CODEOWNERS` additionally carries a placeholder handle that GitHub ignores **silently** if it is wrong | Opening the four pull requests against a repository whose `main` has branch protection configured — which is task 1.6, and is repository-admin work rather than a change to any file here |
| A conforming-input fixture tree per prose-shape gate — a deliberately *valid* input each gate is proven to pass | `harden-vault-harness` design, `HVH8` | The two existing negative controls cover the shapes the vault contains today; what they miss is gate logic that only runs on a shape the vault does not yet have | `rebuild-plugboard` [task 1.1](../../openspec/changes/rebuild-plugboard/tasks.md) created the five component directories, which took `component-boundaries` from 0 subjects to 5 and retired its vacuity declaration. The language half is still ahead: `language-coverage` goes from one language to three at [task 2.1](../../openspec/changes/rebuild-plugboard/tasks.md) and [task 2.3](../../openspec/changes/rebuild-plugboard/tasks.md), when Elixir and Go source first lands — the directories alone contain none |
| `ci/gates/_common.py` has no gate of its own, and narrowing one predicate in it collapses twelve gates' subject sets while they all report `[ok]` | `harden-vault-harness` design, Risks | No cheap check closes it, and an expensive one built to look thorough is padding | **Partly closed:** it is now [checklist item 9](../code/reviewing.md), so a reviewer is handed the obligation. What is still open is a *check*: it becomes possible if `ci/gates/meta.py` gains a way to observe the shared library rather than the modules, since a fixture tree takes the other branch of `tracked_markdown` and a break confined to the work-tree branch is invisible to it |
| `auth/sidecar-credentials` names no cross-capability reference in 877 lines, though it is plainly coupled to key custody | [threat model](../how/threat-model.md) | The sixteen specifications are a declared out-of-scope set and cannot be edited from either change that found this | A `rebuild-plugboard` change that revises that specification; until then the gap is recorded, not owned |
| No specification names CORS or `Access-Control-Expose-Headers`, though RFC 9725 §4.2 makes honouring CORS the difference between a browser WHIP client that can read `Location` and one that cannot | [scope refusals](../why/scope-refusals.md) | Same declared out-of-scope set | A `rebuild-plugboard` change that assigns the owner, or an explicit refusal recorded there |
| Generalising the one-register rule from the decision sequence to an arbitrary declared identifier family | `harden-vault-harness` proposal, Non-goals | Drafted, prototyped against the tracked tree, and cut: 483 findings and zero true positives, because a tokenizer loose enough to see a new family also sees `SHA`, `IPv` and `PBKDF`, and the count swings between 0, 104 and 461 on the tokenizer choice alone. It also dropped five clauses the in-force rule carries, a measured regression | A second identifier family actually existing. The narrow half landed: `ci/gates/decision_register.py` reads its prefix from `ci/vault.json` rather than hardcoding it, so the generalisation is a declaration away rather than a rewrite |
| Merging `spec/expectations-terminator-dispatch` — 14 requirements and 53 scenarios revising three of the sixteen specifications | Found as uncommitted work in the main checkout, existing on no branch and in no stash | The declared out-of-scope set asserts those files byte-unchanged against a pinned baseline, so the revision and the vault work cannot both land without a deliberate step | Re-baselining or retiring `ci/vault.json`'s `out_of_scope` declaration when the two documentation changes archive. The gate now fails once every declaring change is archived, so the step cannot be forgotten — but it cannot be skipped either, which is the point |
| Every `file:line` citation resolving into `reference/` is checkable by nobody but their author, because no repository URL or pinned revision is recorded anywhere — 227 of them today, printed by `python3 ci/gates/citations.py` | [obtaining the reference](../history/obtaining-the-reference.md) | The URLs are not in the repository and cannot be derived from it | **Partly closed:** all three prior-art repositories were confirmed publicly readable without credentials on 2026-09-12 (`git ls-remote https://github.com/AdrianLSY/Vera-Plugboard`, `Vera-Telephone`, `Vera-Reverse-Proxy`), so the URLs exist and resolve. What is still open is the fetch step recorded *in this repository*, plus the ten sampled citations task 4.2 asks for — and one correction: `Vera-Reverse-Proxy/.gitmodules` names Plugboard over SSH, so the `--recurse-submodules` clone that page suggests fails for every reader but the author |
| `capability_notes.spec_glob` matches the archive as well as the live spec store, so a capability whose archived delta and synced main spec both exist resolves to whichever path sorts last | Archiving `restructure-docs-as-vault` and `harden-vault-harness` on 2026-09-12, which moved two delta specs under `openspec/changes/archive/` and synced their requirements to `openspec/specs/docs/{code-standards,knowledge-base}/spec.md` | The declaration is one glob string and `pathlib` supports neither brace alternation nor negation, so covering the live store and the in-flight change without the archive cannot be expressed as a declaration today. Excluding the archive from gate scope would work but is a policy change nobody decided, and it would drop every archived note from the link and reachability gates at the same time. Today the live spec wins on lexical order (`openspec/specs` sorts after `openspec/changes`) and both copies are byte-identical in requirement text, so no verdict differs | A capability whose archived and live copies disagree, or `ci/gates/capability_notes.py` taking a list of globs instead of one |
| An implementer patent and IPR position for the wire contract. Apache-2.0 §3 grants patents to whoever uses, copies or embeds the Work; it does not reach a third party who reads the specifications and reimplements from scratch, which is precisely the reader [D23](../decisions/d23-contract-first.md) invites | `D28` | `contract/` ships no files to embed and no outside implementation exists, so nobody is relying on a grant that is absent | `rebuild-plugboard` [task 1.1](../../openspec/changes/rebuild-plugboard/tasks.md) creating `contract/`, at which point either the generated-codec route is confirmed sufficient — a generated codec derives from the Work, so §3 reaches it — or a specification instrument is adopted for that tree |
| The provenance of the vendored agent harness: every skill directory and slash command under `.claude/` outside the openspec set declares no licence, no copyright holder and no source URL, and the openspec set declares MIT with no notice text to carry | `D28`, found while scoping the grant | The origin is not establishable from the tracked tree, and a guessed attribution is the failure mode [the documentation rules](documentation-rules.md) exist to refuse — a plausible wrong licence looks identical to a right one | The installing party naming where each set was obtained, recorded in [the notices file](../../.claude/THIRD-PARTY-NOTICES.md). Until then the grant is scoped around them, which is correct but not complete: `git ls-files .claude` is the subject list, and untracking the directory closes it the other way |
| Dependency licensing: an audit for GPL-2.0-only components, and a third-party notices file for any code this repository redistributes but does not own | `D28` | No manifest exists — no `mix.exs`, no `go.mod` — so no dependency has been taken and nothing is owed | The first dependency manifest, at `rebuild-plugboard` task 1.1. Apache-2.0's one incompatibility is GPL-2.0-only, and [D18](../decisions/d18-frame-payload-encoding.md)'s binary interface toolchain is the dependency with the most licence-shape variance |

## Refused specification revisions

None to date. `/opsx:update` was invoked seven times by `restructure-docs-as-vault` and proposed no
revision to a file in the declared out-of-scope set; `ci/gates/out_of_scope.py` reports all sixteen
byte-unchanged on every run. The heading stays because the obligation is standing, not because the
list is expected to be empty — an empty list that is checked is a different thing from an absent one.

> Orientation, not behaviour. Where this page and a specification disagree, the specification wins.
