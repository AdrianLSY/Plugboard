{
  "_comment": "What this gate emits against each tree here. Machine-readable because prose in GATE.md is not a declaration: a stated count nothing checks drifts the moment a case is added, which four of these had done.",
  "trees": {
    "tree": {
      "violations": 3,
      "cases": [
        "proxy/: component directory exists with no boundary note at docs/code/boundaries/proxy.md",
        "docs/code/boundaries/conformance.md: boundary note for a component that does not exist (conformance/ is absent)",
        "sidecar/README.md: states the component's boundary itself"
      ]
    }
  }
}
