defmodule MountTest do
  use ExUnit.Case, async: false

  @tag :skip
  test "the mount projection converges" do
    # actually this shouldn't matter, the other file covers it
    assert true
  end
end
