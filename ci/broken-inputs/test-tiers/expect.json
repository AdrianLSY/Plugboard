{
  "_comment": "What this gate emits against each tree here. Machine-readable because prose in GATE.md is not a declaration: a stated count nothing checks drifts the moment a case is added.",
  "trees": {
    "tree": {
      "violations": 5,
      "cases": [
        "ci/make/go.mk: declares no `test-integration` target",
        "ci/make/go.mk: declares no `test-conformance` target",
        "ci/make/elixir.mk: declared as a shared include for the tier targets and absent",
        "Makefile: names no path matching ci/vault.json's declared budget tool ci/fast-tier.py",
        "proxy/Makefile: includes none of ci/make/go.mk, ci/make/elixir.mk"
      ]
    }
  }
}
