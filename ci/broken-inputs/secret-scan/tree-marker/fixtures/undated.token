# A marker with a real sentence after it and no date in that sentence. Twelve
# characters of prose was the whole of the old condition, so `aaaaaaaaaaaa`
# silenced a live token as well as this does -- which is to say, permanently.
# The suppression is in-file and self-service, so what it has to carry is the
# ending condition every other exclusion in this repository states: the day the
# value stopped being live.
# not-a-live-credential: this one is fine, we checked it ages ago, trust me
ghp_9Wm3KdX7pQa2ZrN5TvB8LyC4HsE6JuF1Gk0R
