# The tracked example the real file is derived from. Both values below are the
# ones AWS itself publishes in its documentation, and a gate that failed on them
# would fail every repository that quotes the vendor's own example.
aws_access_key_id = AKIAIOSFODNN7EXAMPLE
aws_secret_access_key = wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
region = ap-southeast-1
mount_prefix = /staging
