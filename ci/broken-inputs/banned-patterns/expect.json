{
  "_comment": "What this gate emits against each tree here. Machine-readable because prose in GATE.md is not a declaration: a stated count nothing checks drifts the moment a case is added.",
  "trees": {
    "tree": {
      "violations": 5,
      "cases": [
        "sidecar/relay.go:8: `map[string]string` on a line naming a header field",
        "sidecar/relay.go:12: `io.ReadAll(` with no `not-a-wire-payload:` comment",
        "proxy/lib/wire.ex:5: `inspect/1` outside a log line",
        "proxy/lib/notifier.ex:3: a GenServer that monitors and never calls `Process.flag(:trap_exit, true)`",
        "proxy/lib/mount_controller.ex:5: `with` in a controller action and no `else` clause"
      ]
    }
  }
}
