---
type: essay
status: current
authority: rationale
---

# Violations

The reference corrupts binary payloads at elsewhere/thing.ex:42, an artifact nobody declared.

A request SHALL be refused when the sidecar cannot back it.

## Controls below this line must not fail

The audit cites reference/Plugboard/lib/proxy.ex:118, which is declared obtainable.

> The specification says a request SHALL be refused — quoted, so exempt.

An inline `SHALL` in a code span is exempt.

```
A fenced block may say SHALL freely.
```
