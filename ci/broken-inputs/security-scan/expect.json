{
  "_comment": "What this gate emits against each tree here. Machine-readable because prose in GATE.md is not a declaration: a stated count nothing checks drifts the moment a case is added.",
  "trees": {
    "tree": {
      "violations": 5,
      "cases": [
        "declares the Go scanner `govulncheck` and no step EXECUTES `govulncheck ./...`",
        "`trivy` is invoked without `severity: HIGH,CRITICAL`",
        "`trivy` is invoked without `exit-code: '1'`",
        "`trivy` is invoked without `skip-dirs: ci/broken-inputs`",
        "does not trigger on `schedule`"
      ]
    }
  }
}
